package com.lucas.financeflow;
public class CategoriasActivity extends AgrupamentoActivity {
    @Override protected boolean porConta() { return false; }
    @Override protected int tabAtual() { return R.id.nav_categorias; }
}
